# Collector subpackage
# Modules: tastytrade_collector, yfinance_collector, fred_collector, vix_utils_collector, news_collector
# Each module exposes a `collect(config, db_conn)` function called by the scheduler.
